services:
  - type: web
    name: keyboard-kev
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    envVars:
      - key: DISCORD_TOKEN
        sync: false
